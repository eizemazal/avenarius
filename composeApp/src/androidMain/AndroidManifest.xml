<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Calls: keep mic/camera capture alive while in a call. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Incoming calls: wake the screen with the call UI and vibrate while ringing. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- QR scanner for web-login authorization. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Recording voice messages. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Suggest address-book contacts when starting a new chat. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />
    <!-- Pre-Q only: DownloadManager writing to the public Downloads folder. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".AvenariusApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="${appLabel}"
        android:supportsRtl="true"
        android:theme="@style/Theme.Avenarius">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:label="${appLabel}"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Avenarius"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Receive photos/videos/files shared from other apps ("Share to Avenarius"). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <!--
              Open max.ru links in the app. No autoVerify: this is a third-party
              client and can't publish assetlinks.json on max.ru, so Android offers
              a choice rather than us claiming the domain outright.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="max.ru" />
                <data android:scheme="https" android:host="*.max.ru" />
                <data android:scheme="http" android:host="max.ru" />
                <data android:scheme="http" android:host="*.max.ru" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- QR scanner (CameraX + ML Kit), pinned to portrait. -->
        <activity
            android:name=".QrScanActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <service
            android:name=".ConnectionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Keeps mic/camera capture alive during a call. -->
        <service
            android:name=".CallService"
            android:exported="false"
            android:foregroundServiceType="microphone|camera" />

        <!-- Shares camera-capture temp files with the system camera app. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
